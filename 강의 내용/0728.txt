this
- 객체(인스턴스) 자기자신의 정보를 가지고 있는 변수
-멤버 변수 또는 메소드에 접근하기 위해서 사용한다.

자기자신에게 해당하는 값이 있으면 자기자신에 우선시
없으면 밖에서 끌어와 쓴다.

private (정보 은닉)
외부의 접근을 허용하지 않겠습니다.
public 이란 키워드를 바꿔서 사용

Source = > Getters and Setters

set = > private 한 변수값을 변경할 때 쓰임
get = > private 한 변수값을 불러와서 다른 변수에 저장할 때 쓰임
