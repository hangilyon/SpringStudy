day05_switch_for
if문으로 switch 문 구현 가능
switch문으로 if문 구현 가능

case (입력) :	break;

case (입력) :	break; 

case (입력) :	break;

default : 

break; 다음엔 어떠한 문장도 쓸 수 없다.

실수는 비교 불가능 , 문자열은 비교 가능하다.

while (true)
종속문장이 참이면, 계속 반복 시켜준다.

switch 의 else는 어떻게 할까? default

for문
for ( 초기값 ; 범위 ; 증감값)
1. 초기값을 본다
2. 조건식
if에서 햇던 내용과 같이 특정 조건식이 들어올 수 있습니다.
해당한 조건이 참이면 종속문장이 실행
3. 증감식
증가하거나 감소시키는 (num = n+1)
		  num++ . num--
4. 다시 조건식으로 돌아와서 해당하는 조건이 참인지 거짓인지 
거짓이면 빠져나온다
; 은 무조건 2개가 들어가야된다.
디버깅 : 연산이 내가 원하는 바로 이루어지는지 이루어지지 않는지
더블클릭 : 브레이크지점
F11 디버깅 시작 모드
F6 한줄 밑으로
종료 Ctrl f2
for문은 조건 없어도 실행 가능

팀별로 한사람당 5문제씩 공유
팀원끼리 공유한 문제는 일요일까지 제출
