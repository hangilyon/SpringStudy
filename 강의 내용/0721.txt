배열 array
컬렉션
배열은 같은 자료형으로 연속된 공간을 만드는 것!

만드는 법 

- 자료형 [] 변수명(배열명);

- 자료형 변수명(배열명)[]; 

int a0;
;;;;
;;;;
;;;;
int a1;
;;;
;;;
;
;;
int a2;
내가 이 변수를 어떠한 용도로 쓸껀지 생각을 해야 됨.
나중에 그 변수를 봤을 때 용도를 알 수 있음.
편리하게 정리 할 수 있게 하는게 배열

int [] arr; = new int[5];
이렇게 arr이라는 변수에 공간이 만들어짐
정수만 저장

int num; num = 10; 공간이 하나라서 하나만 대입함

5개의 공간이면은 [0] [1] [2] [3] [4] 표현 가능

arr[0] = 100; 이런식으로 대입

변수명.length = 공간의 갯수

List
순서가 있는 집합
데이터의 중복을 허용한다.
배열과 다르게 가변 크기를 가지고 있다.
List
- ArrayList
- LinkedList
- Stack
...등이 존재한다.

array 의 사이즈는 항상 고정
ArrayList 는 사이즈 조절 가능
변수명.indexOf(value) 찾는 값 위치
변수명.get( i ) : i 공간에 저장되어있는 데이터는 무엇?
변수명.size() : arr의 공간이 몇개입니까
변수명.contains(i) : i 라는 데이터가 있습니까?  있으면 true  없으면 false 
변수명.remove( ) : 삭제
변수명 : 저장된 모든 데이터
첫번째가 소문자인 자료형은 기능 x 
대문자인 자료형은 기능 0

변수형.set(몇번째,변경할 값);

Set - 자료형
- HastSet
- TreeSet..등등
순서를 유지하지 않는다. 그리고 중복 허용하지 않는다.

Iterator

list 앞에 bof ,뒤에 eof
list 값을 반복자로 변환하겠습니다.
bof 데이터의 시작입니다
eof 데이터의 끝입니다.
it.hasNext() 다음 값이 있습니까 ? 
it.next() 다음 값으로 이동










